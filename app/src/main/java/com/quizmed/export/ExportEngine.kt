package com.quizmed.export

object ExportEngine {
    fun generatePdf(content: String, theme: String): ByteArray {
        // Placeholder for real PDF engine (iText / OpenPDF)
        return content.toByteArray()
    }
}
